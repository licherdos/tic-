#include <stdio.h>

int main()
{
    int numero1;
    printf("inserte numero deseado");
    scanf("%d", &numero1);
    printf("\n");
 
    int i = numero1;
    while(i > 1){
        
        i = i / 2;
        printf("Resultado final es: %d\n", i);
    }
    
    return 0;
}
